import { IncludeInArrayPipe } from './include-in-array.pipe';

describe('IncludeInArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new IncludeInArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
